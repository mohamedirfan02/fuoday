import 'package:flutter/material.dart';

class HomeRecruiterScreen extends StatelessWidget {
  const HomeRecruiterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: AppBar());
  }
}
